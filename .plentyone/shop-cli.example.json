{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "PlentyONE Shop CLI Configuration",
  "description": "Configuration file for the PlentyONE Shop CLI tool",
  "type": "object",
  "properties": {
    "webAppPath": {
      "type": "string",
      "description": "Path to the web application directory (relative to project root)",
      "default": "apps/web/app",
      "examples": [
        "apps/web/app",
        "src/app",
        "custom/app"
      ]
    }
  }
}
