export type PreferredDay = {
  date: string;
  dayNumber: string;
  dayName: string;
};

export type PreferredOption = {
  enabled: boolean;
  checked: boolean;
  value: string;
};

export type NeighbourOption = {
  enabled: boolean;
  checked: boolean;
  name: string;
  address: string;
};
