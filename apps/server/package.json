{
  "name": "server",
  "version": "1.15.0",
  "main": "./src/index.ts",
  "license": "BSD-3-Clause",
  "scripts": {
    "dev": "nodemon",
    "start": "echo \"Starting server...\" && nodemon --spawn",
    "lint": "echo \"No linting configured for server app\"",
    "lint:fix": "echo \"No linting configured for server app\"",
    "build": "tsup --minify"
  },
  "dependencies": {
    "@types/cors": "^2.8.17",
    "@vue-storefront/middleware": "5.3.2",
    "consola": "^3.2.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "nodemon": "^3.1.4",
    "ts-node-dev": "^2.0.0",
    "tsup": "^8.5.0"
  }
}
